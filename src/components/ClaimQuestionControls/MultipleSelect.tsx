import React from 'react'
import { TClaimQuestionControlProps } from './types'
import styled from 'styled-components'

const StyledMultipleSelect = styled.div``

const MultipleSelect: React.FC<TClaimQuestionControlProps> = ({ onChange, ...rest }) => {
  return <StyledMultipleSelect />
}

export default MultipleSelect
