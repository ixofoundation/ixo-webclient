import styled from 'styled-components'

export const EditEntityWrapper = styled.div`
  background: linear-gradient(to bottom, #012639 0%, #002d42 100%);
  border: 1px solid #0c3550;
  box-shadow: 0 2px 10px 0 rgb(0 0 0 / 18%);
  padding: 2rem;

  .buttons-group {
    margin-top: 2rem;
  }

  .step-text {
    color: white !important;
  }
`
