query Evaluations($filter: EvaluationFilter) {
  evaluations(filter: $filter) {
    totalCount
    nodes {
      collectionId
      status
      verificationProof
      claim {
        submissionDate
      }
    }
  }
}